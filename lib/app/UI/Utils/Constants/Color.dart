import 'package:flutter/material.dart';

class SecureEmailCoreColors {
  // Primary & Secondary Colors
  static const Color primary = Color(0xFF0052CC);
  static const Color semiPrimary = Color(0xFF4C9AFF);

  // Backgrounds
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF2C2C2C);
  static const Color gray = Color(0xFFF6F6F6);
  static const Color muted = Color(0xFFF0F2F5);
  static const Color cardBackground = Color(0xFFF8F9FB);
  static const Color background = Color(0xFFEFEFEF);

  // Foreground (Text & Icons)
  static const Color foreground = Color(0xFF2C2C2C);

  // Search & Borders
  static const Color searchBarColor = Color(0xFFF2F2F2);
  static const Color borderColor = Color(0xFFE0E0E0);

  // Shimmer Colors
  static const Color shimmerBaseColor = Color(0xFFE0E0E0);
  static const Color shimmerHighlightColor = Color(0xFFF5F5F5);
  static const Color shimmerForegroundColor = Color.fromARGB(
    255,
    186,
    180,
    180,
  );
}
